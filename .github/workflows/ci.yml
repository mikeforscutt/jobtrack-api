name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:16-alpine
        env:
          POSTGRES_USER: postgres
          POSTGRES_DB: jobtrack_test
          POSTGRES_HOST_AUTH_METHOD: trust
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - run: npm ci

      - name: Set up test database schema
        run: |
          PGPASSWORD= psql -h localhost -U postgres -d jobtrack_test -f init-db/init.sql

      - run: npm test
        env:
          NODE_ENV: test
          TEST_DATABASE_URL: postgres://postgres@localhost:5432/jobtrack_test
          JWT_SECRET: test-secret